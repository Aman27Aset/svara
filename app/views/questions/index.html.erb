<p id="notice"><%= notice %></p>



<center>
<% @questions.each do |question| %>
<div class="jumbotron">
  <div class="container">
    <h3><%= link_to question.body, question %></h3>
    <h4>Spurt av <%= question.user.username %> in <span class="question-category"><%= question.category %></h4></span> 
    
    <%= link_to 'Svar', new_question_answer_path(question), class: "btn btn-danger btn-lg" %>
    <%= link_to 'Se på svarene',question, class: "btn btn-success btn-lg" %>
    <% if current_user.id == question.user.id %>
      <div>
        <%= link_to 'edit', edit_question_path(question), class: 'btn btn-warning btn-xs' %>
        <%= link_to 'Destroy', question, method: :delete, data: { confirm: 'Are you sure?'}, class: 'btn btn-warning btn-xs' %>
      </div>
    <% end %>  
  </div>
</div>
<% end %>
</center>
        